import React, { useState } from "react";
import { Button } from "react-bootstrap";

const Products = () => {

  return (
    <div>
      <h1 className="text-center">Welcome to the Products component</h1>
    </div>
  );
};

export default Products;