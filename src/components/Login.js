import React, { useState } from "react";
import { Button } from "react-bootstrap";

const Login = () => {

  return (
    <div>
      <h1 className="text-center">Welcome to the Login component</h1>
    </div>
  );
};

export default Login;