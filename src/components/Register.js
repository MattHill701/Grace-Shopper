import React, { useState } from "react";
import { Button } from "react-bootstrap";

const Register = () => {

  return (
    <div>
      <h1 className="text-center">Welcome to the Register component</h1>
    </div>
  );
};

export default Register;